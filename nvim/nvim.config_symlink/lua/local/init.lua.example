-- Copy this file to lua/local/init.lua (gitignored) and set your profile.
-- 'work'  → GitHub Copilot + CopilotChat (default, no file needed on work machines)
-- 'home'  → claudecode.nvim via claude-sandbox (Docker, Claude Pro, per-project memory)
--           Requires: Docker Desktop, brew install socat jq
return {
  profile = 'work',
}
